# Repository Cleanup Report

**Date:** 2026-05-03
**Scope:** Post-Wave-6 final cleanup and validation pass

---

## What Was Scanned

All directories and files in the repository root, including:

- `app/` — FastAPI backend (58 Python files)
- `ui/` — Streamlit UI (13 Python files)
- `tests/` — Unit, integration, and E2E tests
- `docs/` — Documentation
- `static-demo/` — Portfolio static site
- `notebooks/` — Jupyter notebooks
- `observability/` — Prometheus and Grafana configs
- `alembic/` — Database migrations
- `Doces/`, `AI-System-Templates-Library/`, `Project-Blueprint-System/` — Source of truth (read-only)
- Root-level config files

Artifacts checked:
- `__pycache__` directories
- `.pytest_cache`
- `.mypy_cache`, `.ruff_cache` (not found)
- `htmlcov` (not found)
- `.coverage` (not found)
- Backup/temp files (`*.bak`, `*.tmp`, `*.orig`, `~`)
- Log files
- Duplicate or obsolete reports

---

## What Was Removed

| Item | Reason |
|---|---|
| 19 × `__pycache__/` directories | Regenerated by Python at runtime; not committed to VCS |
| `.pytest_cache/` | Regenerated by pytest at runtime; not committed to VCS |

These are listed in `.gitignore` and are safe to remove. Python and pytest recreate them automatically on the next run.

---

## What Was Kept Intentionally

| Item | Reason |
|---|---|
| `app/` | Core FastAPI backend — active application code |
| `ui/` | Streamlit UI — active application code |
| `tests/` | Full test suite — required for validation |
| `docs/` (14 files) | Architecture, API, deployment, security, and portfolio docs |
| `static-demo/` | Portfolio static site — required for demo |
| `notebooks/` | Conceptual demo notebooks |
| `observability/` | Prometheus config + Grafana dashboard provisioning |
| `alembic/` | Database migrations — required for schema management |
| `Doces/` | Source of truth planning documents (read-only per CLAUDE.md) |
| `AI-System-Templates-Library/` | Template library (read-only per CLAUDE.md) |
| `Project-Blueprint-System/` | Blueprint templates (read-only per CLAUDE.md) |
| `.env.example` | Environment configuration template |
| `docker-compose.yml` | Full stack orchestration |
| `Dockerfile`, `Dockerfile.ui` | Container image definitions |
| `.streamlit/config.toml` | Streamlit configuration (newly added) |
| `requirements.txt` | Python dependency list |
| `pyproject.toml` | Project metadata and pytest config |
| `CLAUDE.md` | Engineering contract |
| `README.md` | Main project documentation |
| `.gitignore` | VCS ignore rules |

---

## What Needs Human Approval

None. All removed items were standard Python/pytest cache artifacts that are:
1. Listed in `.gitignore`
2. Automatically regenerated on the next run
3. Not application code, test code, documentation, or configuration

No application logic, tests, migrations, documentation, or configuration was removed.

---

## Confirmation: No Core App Logic Removed

- Zero `.py` source files deleted
- Zero test files deleted
- Zero migration files deleted
- Zero documentation files deleted
- Zero Docker or config files deleted
- Only regeneratable cache directories removed

---

## Also Fixed in This Pass (not deletions)

| Issue | Fix |
|---|---|
| `SettingsError` on `ALLOWED_SCAN_PATHS=` | Added custom pydantic_settings source patching + `extra='ignore'` |
| Streamlit auto-nav showing page files | Added CSS `[data-testid='stSidebarNav']{display:none}` in `ui/main.py` |
| `st.components.v1.html` deprecation warning | Replaced with `st.html()` (Streamlit ≥ 1.36) with fallback |
| Missing `.streamlit/config.toml` | Created with `browser.gatherUsageStats = false` |
| DB healthcheck missing `-d` flag | Added `-d ${POSTGRES_DB}` to `pg_isready` in docker-compose.yml |
| Static demo port inconsistency | Updated README: `python -m http.server 8012` (was 8099) |
| Stale Docker volume FATAL error | Added documentation in README about `docker compose down -v` |
